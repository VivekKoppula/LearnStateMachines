<UserControl x:Class="AppccelerateStateMachine.Views.UserControl1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AppccelerateStateMachine"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Label
            FontWeight="Bold"
            Margin="10,10,0,0" Content="View 1" />
        <StackPanel VerticalAlignment="Center">
            <Button
                Content="Go to View 2"
                Command="{Binding GoTo2}"
                Width="90" Height="30"
                Margin="0, 0, 0, 20"/>

            <Button
                Content="Go to View 3"
                Command="{Binding GoTo3}"
                Width="90" Height="30" />

        </StackPanel>
    </Grid>
</UserControl>
